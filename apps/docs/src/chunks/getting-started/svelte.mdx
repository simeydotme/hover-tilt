---
title: Getting Started (svelte)
---

import { Tabs, TabItem, Code } from '@astrojs/starlight/components';
import CodeWithPreview from '@components/astro/CodeWithPreview.astro';
import BasicExampleSvelte from './BasicExampleSvelte.astro';

export const example = String.raw`
<script>
  import { HoverTilt } from 'hover-tilt';
</script>

<HoverTilt>Your content here</HoverTilt>
`;

export const exampleWithStyling = String.raw`
<script>
  import {HoverTilt} from 'hover-tilt';
</script>

<HoverTilt tiltFactor={1.5} scaleFactor={1.1}>
  <div class="card">
    <h2>Card Title</h2>
    <p>Hover over this card to see the effect!</p>
  </div>
</HoverTilt>

<style>
  .card {
    width: 300px;
    height: 200px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    border-radius: 12px;
    padding: 2rem;
    color: white;
  }
</style>

`

#### Import and Use

Import the `HoverTilt` component in your Svelte file:

<Code lang="svelte" title="App.svelte" code={example} />

This would look pretty bad, though, so let's add a bit of styling to make it look better:

#### Basic Example

The content of the `<HoverTilt />` component is passed as a slot, and can be anything you want.

<CodeWithPreview lang="svelte" title="MyComponent.svelte" code={exampleWithStyling}>

<div class="mx-auto max-w-100 text-center">
  <BasicExampleSvelte />
</div>

</CodeWithPreview>
