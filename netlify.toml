[build]
  base = "/"
  publish = "apps/docs/dist"
  command = "pnpm install --frozen-lockfile --include-workspace-root && pnpm --filter ./apps/docs build"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "10"

